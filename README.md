# My Remingan – React Portfolio

## Run
1. Install Node.js (v18+ recommended)
2. In terminal:
```bash
npm install
npm run dev
```
Open the printed local URL in your browser.

## Replace Photo & Certificates
- Replace `src/assets/photos/me.jpg` with your real photo (keep the same name).
- Add more certificate images inside `src/assets/certificates/` and list them in `src/data/certificates.json`.

## Notes
- Pure CSS in `src/styles/*.css`
- Animations via Framer Motion, smooth scroll via react-scroll.
- Exactly 7 skills are defined in `src/data/skills.json`.
